export const chartConfig = {
  primary: "var(--primary)",

  accent: "var(--accent)",

  foreground: "var(--foreground)",

  muted: "var(--muted-foreground)",

  border: "var(--border)",

  card: "var(--card)",
  margin: {
    top: 20,
    right: 20,
    bottom: 20,
    left: 20,
  },

  tooltip: {
    cursor: false,
  },

  animation: {
    duration: 800,
  },

  axis: {
    stroke: "transparent",
    tickLine: false,
    axisLine: false,
  },
};
